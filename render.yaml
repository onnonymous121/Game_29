services:
  - type: web
    name: game29-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node main_server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000